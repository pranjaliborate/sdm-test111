create table Onlinebooking (
bookingNo int primary key,
Bookingname varchar(30) not null,
loc varchar(30));


create table Airway(
wayNo int primary key,
wayname varchar(30),
wayloc varchar(20)
);


create table customer(
custno int primary key,
custName varchar(20),
cusloc varchar(20)
);

 create table banking (
 bnkgidno int primary key,
 bankname varchar(20),
 bankloc varchar(20)
 );
 
 


